# App
NEXT_PUBLIC_SITE_URL="http://localhost:3000"
NEXT_PUBLIC_WHATSAPP="5511999999999"

# Auth (gere com: openssl rand -base64 32)
AUTH_SECRET="troque-por-um-segredo-forte"
NEXTAUTH_URL="http://localhost:3000"

# E-mail transacional (Resend)
RESEND_API_KEY="re_..."
RESEND_FROM_EMAIL="Selavie Femme <contato@seudominio.com.br>"

# PostgreSQL (Prisma 7)
# App / Prisma Client — pooler (transaction mode), porta 6543 no Supabase
DATABASE_URL="postgresql://USER:PASSWORD@host:6543/postgres?pgbouncer=true"
# CLI / migrations — conexão direta (session), porta 5432
DIRECT_URL="postgresql://USER:PASSWORD@host:5432/postgres"

# Mercado Pago PIX (opcional — sem token usa PIX direto pela chave)
MERCADOPAGO_ACCESS_TOKEN=""
NEXT_PUBLIC_MERCADOPAGO_PUBLIC_KEY=""

# Chave PIX que recebe os pagamentos (e-mail, CPF, CNPJ, telefone ou EVP)
PIX_KEY="henrymaximo099@gmail.com"
PIX_MERCHANT_NAME="SELAVIE FEMME"
PIX_MERCHANT_CITY="SAO PAULO"

# Cópia do pedido pago (além do e-mail do cliente)
ORDER_NOTIFY_EMAIL="henrymaximo099@gmail.com"
