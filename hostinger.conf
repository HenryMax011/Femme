# Hostinger — deploy STANDALONE (sem build no servidor)
# Contorna EACCES: o app já vem compilado.

framework = "nextjs"
node = "22"
install_command = "true"
build_command = "true"
start_command = "node server.js"
output_directory = ""
